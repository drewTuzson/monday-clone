# Production Environment Variables for Vercel/Railway deployment

# Server Configuration
NODE_ENV=production
PORT=4000

# Database Configuration (Update with your cloud database URL)
DATABASE_URL=postgresql://username:password@hostname:port/database

# Redis Configuration (Update with your cloud Redis URL)
REDIS_URL=redis://username:password@hostname:port

# JWT Configuration (Generate secure secrets)
JWT_SECRET=your-production-jwt-secret-key-256-bits-long
JWT_REFRESH_SECRET=your-production-refresh-secret-key-256-bits-long
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS Configuration (Update with your frontend URL)
CORS_ORIGIN=https://your-app-name.vercel.app